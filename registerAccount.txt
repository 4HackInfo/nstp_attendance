from accounts.models import User

# Create Instructor
instructor = User.objects.create_user(
    username='instructor1',
    password='instructor123',
    email='instructor@nstp.edu',
    user_type='instructor',
    first_name='John',
    last_name='Doe'
)

# Create Task Force Admin
taskforce = User.objects.create_user(
    username='taskforce1',
    password='taskforce123',
    email='taskforce@nstp.edu',
    user_type='admin_staff',
    first_name='Jane',
    last_name='Smith'
)

exit()

admin
admin123